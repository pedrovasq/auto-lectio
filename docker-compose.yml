version: "3.9"
services:
  auto-lectio:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./out:/app/out
      - ./build:/app/build
      - ./templates:/app/templates
    environment:
      - PORT=8000
    restart: unless-stopped

